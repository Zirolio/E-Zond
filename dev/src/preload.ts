import { ipcRenderer } from "electron";

if (document.location.href !== "about:blank") {
    /* ipcRenderer.sendSync("aaa", window.__EZOND__, !window.__EZOND__)
    if (!window.__EZOND__) {
        console.log(123);
        // window.__EZOND__ = true;
    } */
    // ipcRenderer.send("run-script");
}