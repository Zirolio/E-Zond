import Replacer from "@injector/Replacer";

export default class UpdateMessageReplacer extends Replacer {
    /* -eslint-disable */
    /* -eslint-enable */
    
    constructor() {
        super();

    }
}